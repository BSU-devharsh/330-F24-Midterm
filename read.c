// Write your name here

#include <stdio.h>

int main() {
    FILE *file;
    char ch, choice;
    int lines = 0, words = 0, chars = 0;

    printf("Enter 1 to count lines, words, and characters.\n");
    printf("Enter 2 to simply print the file.\n");
    scanf(" %c", &choice);

    // Write your code here
    // ...
    
    fclose(file);
    return 0;
}
